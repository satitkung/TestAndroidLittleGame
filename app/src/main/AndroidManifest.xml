<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.test">

    <application
            android:name=".AppApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".dragAndDropMenuTwoRCV.DragAndDropTwoRCVActivity"></activity>
        <activity android:name=".dragAndDropMenu.DragAndDropMenuActivity" />
        <activity
                android:name=".DrawLine.DrawLineActivity"
                android:label="@string/title_activity_draw_line"
                android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Elevator.ElevatorActivity" />
        <activity android:name=".Elevator.MainElevatorActivity" />
        <activity android:name=".MainWarmUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CountGame.CountActivity" />
    </application>

</manifest>